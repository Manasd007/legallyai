# Only backend/ goes into the image — keep the build context tiny.
# (The voice service is a separate process with its own deploy; see
# voice/Dockerfile. It must not ride along in the backend image.)
frontend/
voice/
pipeline/
samples/
sql/
docs/
huggingface/
data/
.claude/
.git/
**/__pycache__/
**/.venv/
**/*.pyc
backend/.env
.env*
!*.env.example
README.md
