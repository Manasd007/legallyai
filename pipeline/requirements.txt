# Phase 0 offline pipeline (run on Kaggle/Colab for the full corpus)
boto3>=1.34            # anonymous S3 access to the AWS Open Data bucket
pymupdf>=1.24          # fast PDF text extraction (pypdf is a fallback)
pypdf>=4.0
pandas>=2.2
pyarrow>=15.0          # read parquet metadata

# Embedding + index build (chunk_embed.py)
transformers>=4.40
torch>=2.2
faiss-cpu>=1.8
numpy>=1.26
